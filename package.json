{
  "name": "parallel-test-project",
  "version": "1.0.0",
  "main": "index.js",  // Remove the comment here
  "scripts": {
    "test": "jest --ci"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
